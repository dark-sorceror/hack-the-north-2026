📋 QUICK START: Run Microphone → VLA → Detection on Pi
═════════════════════════════════════════════════════════════

🟢 OPTION 1: Simple One-Liner (Copy & Paste)
─────────────────────────────────────────────

ssh gisooj@gisoopi.local "cd ~/VLA-HTN && source venv/bin/activate && python test_microphone_vla_detection.py"


🟢 OPTION 2: Step-by-Step Commands
──────────────────────────────────

# 1. Copy test file to Pi
scp test_microphone_vla_detection.py gisooj@gisoopi.local:~/VLA-HTN/

# 2. SSH to Pi
ssh gisooj@gisoopi.local

# 3. On the Pi, run these:
cd ~/VLA-HTN
source venv/bin/activate
pip install -e '.[camera,audio,camera-realsense]'  # One-time dependency install
python test_microphone_vla_detection.py


🟢 OPTION 3: Multiple Tests in Sequence
────────────────────────────────────────

# First test (mic → VLA decomposition)
ssh gisooj@gisoopi.local 'cd ~/VLA-HTN && source venv/bin/activate && python test_microphone_vla_detection.py'

# Then test live camera
ssh gisooj@gisoopi.local 'cd ~/VLA-HTN && source venv/bin/activate && python test_camera_on_pi.py'

# Then test with voice coordinator
ssh gisooj@gisoopi.local 'cd ~/VLA-HTN && source venv/bin/activate && python -m robot_app.voice'


═════════════════════════════════════════════════════════════
              WHAT EACH COMMAND TESTS
═════════════════════════════════════════════════════════════

test_microphone_vla_detection.py
  ✓ Simulates voice command: "detect a bottle"
  ✓ Calls VLA task decomposer (Qwen 3.5)
  ✓ Captures frame from RealSense camera
  ✓ Runs YOLO World object detection
  ✓ Reports detection confidence and bounding box

test_camera_on_pi.py
  ✓ Tests RealSense camera hardware
  ✓ Tests YOLO object detection
  ✓ Tests camera calibration

voice.py (robot_app.voice)
  ✓ Runs full voice-to-robot pipeline
  ✓ Live microphone input (Qwen voice server)
  ✓ Tool calling (fetch_object, stop_robot)
  ✓ Robot coordination


═════════════════════════════════════════════════════════════
              EXPECTED OUTPUT
═════════════════════════════════════════════════════════════

======================================================================
LIVE TEST: Microphone Command → VLA → Object Detection
======================================================================

[1/4] MICROPHONE INPUT SIMULATION
──────────────────────────────────────────────────────────────────────
🎤 Simulated microphone input: 'detect a bottle'

[2/4] VLA TASK DECOMPOSITION
──────────────────────────────────────────────────────────────────────
📊 Initializing VLA decomposer (model: qwen3.5-plus)
✓ Task plan generated:
  Target object: a bottle
  Confidence: 1.0
  Subgoals: 4 steps
    1. navigate
    2. approach_arm
    3. grasp
    4. deliver

[3/4] CAMERA CAPTURE
──────────────────────────────────────────────────────────────────────
✓ Frame captured: 640x480 pixels

[4/4] OBJECT DETECTION
──────────────────────────────────────────────────────────────────────
🔍 Detecting 'a bottle' in frame...
✓ DETECTION SUCCESSFUL!
  Object: a bottle
  Bounding box: (150.2, 120.5) → (450.8, 380.3)
  Confidence: 85.00%
  Width: 300.6px
  Height: 259.8px

====================================================================
TEST SUMMARY
====================================================================
✓ Pipeline complete: Microphone → VLA → Object Detection

Full pipeline tested:
  1. Simulated microphone: Captured voice command
  2. VLA task decomposer: Generated task plan from command
  3. Camera service: Captured real-time frame
  4. Object detection: Applied YOLO World model to frame


═════════════════════════════════════════════════════════════
              TROUBLESHOOTING
═════════════════════════════════════════════════════════════

❌ "ModuleNotFoundError: No module named 'robot_app'"
   → Run: pip install -e '.[camera,audio,camera-realsense]'

❌ "No RealSense cameras detected"
   → Check: usb lsb | grep RealSense
   → Run: lsusb to see all USB devices

❌ "Cannot find yolov8s-worldv2.pt"
   → Copy weights: scp yolov8s-worldv2.pt gisooj@gisoopi.local:~/VLA-HTN/

❌ "Permission denied" on ssh
   → Make sure ssh keys are set up or password auth works

❌ YOLO detection returns None
   → Model may need to warm up
   → Check camera frame isn't all black
   → Try: python -c "from ultralytics import YOLOWorld; model = YOLOWorld('yolov8s-worldv2.pt'); print(model)"


═════════════════════════════════════════════════════════════
              ARCHITECTURE
═════════════════════════════════════════════════════════════

┌─────────────┐
│ Microphone  │  (simulated: "detect a bottle")
└──────┬──────┘
       │
       ▼
┌──────────────────────┐
│  VLA Decomposer      │  (Qwen 3.5 Plus via DashScope)
│  (task_decomposer.py)│  → generates task plan with subgoals
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Camera Service      │  (camera.py)
│  (RealSense D455)    │  → captures 640x480 frame
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Object Detection    │  (vla_core/object_detection)
│  (YOLO World v2)     │  → detects target object
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Bounding Box        │  → [x1, y1, x2, y2, confidence]
│  Detection Result    │  → used by robot coordinator
└──────────────────────┘

